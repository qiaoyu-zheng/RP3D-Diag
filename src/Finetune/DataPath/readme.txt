Train / Eval files should be added to this folder:

Required:
--"train": train.json
--"test": test.json
--"label": disorder_label_dict.json / icd10_label_dict.json

Furthermore, you need to change the class number in train.sh to fit this dataset.
