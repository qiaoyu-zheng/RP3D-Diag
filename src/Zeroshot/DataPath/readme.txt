Train / Eval files should be added to this folder:

Required:
--"train": train.json
--"test": test.json
--"label": disorder_label_dict.json / icd10_label_dict.json

However the train.json will not be used during the flow.

Class number need to be set to 5569 to align with the model checkpoint.

