After downloading from huggingface repository, you shuold add the following files to this folder:
    1. RP3D_train.json
    2. RP3D_test.json
    3. disorder_label_dict.json
    4. icd10_label_dict.json

"""
Pay attention that: in RP3D_{train/test}.json file. A 'url' key corresponds to a 'Case', and there maybe multiple 'samples' in a 'Case' under the 'Samples' key
"""
